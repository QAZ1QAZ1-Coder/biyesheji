<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AddressMapper">

    <insert id="insert" parameterType="com.example.entity.Address" useGeneratedKeys="true" keyProperty="id">
        insert into address(user_id, consignee, phone, province, city, district, detail, is_default)
        values (#{userId}, #{consignee}, #{phone}, #{province}, #{city}, #{district}, #{detail}, #{isDefault})
    </insert>

    <update id="updateById" parameterType="com.example.entity.Address">
        update address set
                           consignee = #{consignee},
                           phone = #{phone},
                           province = #{province},
                           city = #{city},
                           district = #{district},
                           detail = #{detail},
                           is_default = #{isDefault}
        where id = #{id}
    </update>

    <!-- 查询所有地址（管理员专用） -->
    <select id="selectAllWithUser" resultMap="AddressWithUserMap">
        SELECT
            a.*,
            u.name as user_name
        FROM address a
                 LEFT JOIN user u ON a.user_id = u.id
        ORDER BY a.create_time DESC
    </select>

    <!-- 定义结果映射 -->
    <resultMap id="AddressWithUserMap" type="com.example.entity.Address">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="consignee" column="consignee" />
        <result property="phone" column="phone" />
        <result property="province" column="province" />
        <result property="city" column="city" />
        <result property="district" column="district" />
        <result property="detail" column="detail" />
        <result property="isDefault" column="is_default" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="userName" column="user_name" />
    </resultMap>

</mapper>